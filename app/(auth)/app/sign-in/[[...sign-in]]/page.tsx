"use client";
import React from "react";
import { SignIn } from '@clerk/nextjs';

export default function DeprecatedNestedSignIn() {
  // Deprecated nested route — intentionally returns null to avoid duplicate routes.
  return null;
}